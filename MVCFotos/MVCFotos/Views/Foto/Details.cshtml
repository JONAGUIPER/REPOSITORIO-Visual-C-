@model MVCFotos.Models.Foto
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<h2>Detalle de Foto</h2>
<div class="row">
    <div class="col-md-4">
        <img class="thumbnail" src="@Html.DisplayFor(c=>Model.FotoURL)" />
    </div>
    <div class="col-md-8">
        <dl class="dl-horizontal">
            <dt>@Html.DisplayNameFor(model => model.IDFoto)</dt>
            <dd>@Html.DisplayFor(model => model.IDFoto)</dd>
            <dt>@Html.DisplayNameFor(model => model.Titulo)</dt>
            <dd>@Html.DisplayFor(model => model.Titulo)</dd>
            <dt>@Html.DisplayNameFor(model => model.FechaCreacion)</dt>
            <dd>@Html.DisplayFor(model => model.FechaCreacion)</dd>
        </dl>
    </div>
</div>


@*@Html.Action("_CommentsForPhoto", "Comentario", new { id = Model.IDFoto })*@
    @Html.Action("_CommentsForPhotoAjax", "Comentario", new { id = Model.IDFoto })


@*<h2>COMENTARIOS MIOS</h2>
<div id="comentarios">
    @Html.Action("_Comentarios", "Comentario", new { IDFoto = Model.IDFoto })
</div>


<div class="row">
    <div class="col-md-2 col-md-offset-5">
        @using (Html.BeginForm("AgregarComentario", "Comentario", new { id = Model.IDFoto }, FormMethod.Get))
        {
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Agregar Comentario" class="btn btn-default" />
                </div>
            </div>
        }
    </div>

</div>*@

@*@Html.Action("_Create", "Comentario", new { id = Model.IDFoto })*@

@*@Html.Action("_AgregarComentario", "Comentario", new { id = Model.IDFoto })*@


